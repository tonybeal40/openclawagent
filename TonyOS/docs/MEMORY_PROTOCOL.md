# MEMORY_PROTOCOL

- Do not keep large historical context in chat.
- Persist everything to structured files in `TonyOS/memory`.
- Retrieve only relevant records before reasoning (layered memory / retrieval-first).
- Update `relationships.json` whenever new entities connect.
